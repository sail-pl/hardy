input i : bool;
output o : bool;
var prev_i : bool;
guarantees X G {o <-> (!(prev i)) && i}
guarantees G {i = prev_i}

SETUP :
    ensures { prev_i = false }
{
  prev_i := false;
}  

START :
{
    emit (i && !prev_i) to o;
    prev_i := i;
}
