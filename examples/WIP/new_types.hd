output z: int array
;
var 
    x : (int*int) array 
    y : real
;


setup:
 //ensures {length x = 2} 
 // required to show x[1] is in bound

 x := [|(0,0);(0,0)|];

loop:
    invariant { z[0] = 1 && z[1] = 2 && z[2] = 3  }
    x[1] := 2,3;
    emit [|1;2;3|] to z;