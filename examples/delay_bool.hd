input i : bool;
output o : bool;
var x : bool; 

guarantees ! {o}.
guarantees G {x = i}.
guarantees G ({i} -> X {o}).
guarantees G (!{i} -> X !{o}).

START:
    ensures { x = false }
    {
        x := false;
    }
GOTO LOOP

LOOP:
    {
    emit x to o;
    x := i;
    }
