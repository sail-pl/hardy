input i : bool;
output o : bool;
var x : bool; 

guarantees ! {o}
guarantees G {x = i}
guarantees G ({i} -> X {o})
guarantees G (!{i} -> X !{o})

START:
| _ {
    x := i;
} => LOOP

LOOP:
{
    emit x to o;
    x := i;
}
