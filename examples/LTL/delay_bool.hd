input i : bool;
output o : bool;
var x : bool; 

guarantees !{o}
guarantees G ({i} -> X {o})
guarantees G ({!{i} -> X !{o})

setup:
    ensures { x = false }
    x := false;

loop:
    invariant {x = i}
    emit x to o;
    x := i;
