input i : int;
output o : int;
var x : array<int;3>;

guarantees X X X G {o = prev 3 i}

START :
| _ {
    x := [|0;1;2|];
} => LOOP

LOOP :
{
    emit x[0] to o;
    x[0] := x[1];
    x[1] := x[2];
    x[2] := i;
}