input i : int;
output o : int;


// malformed spec
// relies on {o=3} 

// but output can still be mentionned as a way for the input to depend on it, particularly useful, for composition


guarantees G {o=3}

START :
    // malformed program
 { emit o to o; }

